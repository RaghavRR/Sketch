# dependencies
/node_modules
/.pnp
.pnp.*
.yarn/*
!.yarn/patches
!.yarn/plugins
!.yarn/releases
!.yarn/versions

# testing
/coverage

# next.js build outputs (ignore local build output)
/.next/
/out/

# production build output
/build

# misc system files
.DS_Store
*.pem

# debug logs
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.pnpm-debug.log*

# environment variables - ignore by default, commit only if intentional
.env*

# Vercel config folder (optional to ignore)
/.vercel

# typescript incremental build info
*.tsbuildinfo

# Next.js environment types (generated by Next)
next-env.d.ts
